shadowFrustumCulling=false
shadow.culling=false
attribute.at_midBlock=at_midBlock
attribute.mc_midTexCoord=mc_midTexCoord
clouds=off

blend.gbuffers_entities=off
blend.gbuffers_hand=off
blend.gbuffers_hand_water=off
blend.gbuffers_particles=off
blend.gbuffers_water=off

# Distant Horizons LOD passes. dh_terrain writes the deferred g-buffer (lit in d7);
# dh_water writes only water normal + flag and is shaded later by c_water. Both
# blend OFF (dithered near-edge fade hands off to vanilla). Gated by
# DISTANT_HORIZONS inside the programs.
blend.dh_terrain=off
blend.dh_water=off

screen=[Light] [Terrain] [Water] [PBR] [Volumetric-Light] [Atmosphere] [Clouds] [Post-Processing] [Path-Tracing]
screen.columns=1

screen.Water=WATER_WAVES WATER_WAVE_OCTAVES WATER_WAVE_AMPLITUDE WATER_WAVE_SPEED WATER_NORMAL_STRENGTH WATER_NORMAL_FADE WATER_NORMAL_FADE_MIN WATER_REFRACTION WATER_REFRACTION_STRENGTH WATER_REFLECTIONS WATER_REFLECTION_STEPS WATER_SKYLIGHT_THRESHOLD <empty> WATER_FOG WATER_ABSORPTION_R WATER_ABSORPTION_G WATER_ABSORPTION_B WATER_SCATTER_R WATER_SCATTER_G WATER_SCATTER_B <empty> WATER_GODRAYS WATER_GODRAY_STEPS WATER_GODRAY_STRENGTH WATER_GODRAY_PHASE_G

screen.Light=SHADOW_RESOLUTION SHADOW_DISTORT_FACTOR SHADOW_BIAS SCREENSPACE_SHADOWS sunPathRotation <empty> [Shadow-Filter] [Subsurface-Scattering]
screen.Shadow-Filter=SHADOW_FILTER DITHER_FILTER SHADOW_FILTER_QUALITY
screen.Subsurface-Scattering=FAKE_SSS SCATTER_AMOUNT
screen.Volumetric-Light=VOLUMETRIC_LIGHT VL_STEPS VL_INTENSITY VL_NOON_STRENGTH VL_SUN_RISE_SET_STRENGTH VL_NIGHT_STRENGTH

screen.Atmosphere=SKY_LUT_STEPS SUN_ILLUMINANCE MOON_ILLUMINANCE MIE_G <empty> RAYLEIGH_SCATTER_R RAYLEIGH_SCATTER_G RAYLEIGH_SCATTER_B <empty> MIE_SCATTER_R MIE_SCATTER_G MIE_SCATTER_B <empty> OZONE_PEAK

screen.Clouds=CLOUDS CLOUDS_COVERAGE CLOUDS_DENSITY <empty> CLOUDS_LAYER_BOTTOM CLOUDS_LAYER_TOP <empty> CLOUDS_WIND_SPEED CLOUDS_WIND_DIR_X CLOUDS_WIND_DIR_Z <empty> CLOUDS_PRIMARY_STEPS CLOUDS_LIGHT_STEPS CLOUDS_MS_OCTAVES CLOUDS_MAX_DISTANCE CLOUDS_MIN_TRANSMITTANCE <empty> CLOUDS_SHADOW CLOUDS_SHADOW_STEPS CLOUDS_SHADOW_EXTENT

screen.Terrain=WIND_MOVEMENT <empty> DISTANT_HORIZONS DH_FOG

screen.PBR=INTEGRATED_PBR SPECULAR_REFLECTIONS <empty> METAL_SMOOTHNESS METAL_ROUGHNESS_VARIATION <empty> PBR_GEN_NORMALS PBR_NORMAL_STRENGTH PBR_DIELECTRIC_NORMAL <empty> SPECULAR_SUN SPECULAR_SUN_STRENGTH PBR_DIELECTRIC_SUN PBR_DIELECTRIC_REFLECT REFLECTION_SKY_STRENGTH REFLECTION_SKY_FADE <empty> REFLECTION_RAYS REFLECTION_STEPS REFLECTION_REFINE_STEPS <empty> REFLECTION_DENOISE REFLECTION_ACCUM_FRAMES REFLECTION_SPATIAL_RADIUS REFLECTION_FIREFLY_CLAMP <empty> REFLECTION_SMOOTHNESS_MIN <empty> PBR_DEBUG

screen.Post-Processing=BLOOM BLOOM_STRENGTH CINEMATIC_LENS_FLARE LENS_FLARE_STRENGTH LENS_FLARE_THRESHOLD LENS_FLARE_STREAK LENS_FLARE_GHOSTS LENS_FLARE_HALO LENS_FLARE_STARBURST CHROMATIC_ABERRATION CHROMATIC_ABERRATION_STRENGTH EXPOSURE AUTO_EXPOSURE AUTO_EXPOSURE_TARGET AUTO_EXPOSURE_SPEED AUTO_EXPOSURE_CENTER_WEIGHT AUTO_EXPOSURE_MIN AUTO_EXPOSURE_MAX TONEMAP_OPERATOR COLOR_CONTRAST COLOR_SATURATION COLOR_TEMP PURKINJE_EFFECT PURKINJE_STRENGTH HALATION HALATION_STRENGTH SPLIT_TONING SPLIT_TONING_STRENGTH FILM_GRAIN_I OVERWORLD_LUT OVERWORLD_LUT_I VIGNETTE [TAA]
screen.TAA=TAA RENDER_SCALE UPSCALE_MODE TAA_JITTER_SCALE TAA_BLEND_WEIGHT TAA_SHARPNESS <empty>

screen.Path-Tracing=VOXEL_GI VOXEL_DISTANCE VOXEL_SHAPES GI_BOUNCE_SHADOWMAP GI_DEBUG_VIEW GI_DENOISE LIGHTING_DIRECT LIGHTING_INDIRECT PT_RASTER_AMBIENT_FLOOR LIGHTING_AO_FULL GI_SAMPLES GI_RADIUS GI_STRENGTH GI_SKY_BRIGHTNESS GI_SKY_WARMTH SKY_LUT_STEPS GI_BOUNCE_SKY GI_SKY_PROBE_DIST GI_EMISSION GI_ACCUM_FRAMES GI_TEMPORAL_REJECT <empty> [SVGF] [ReSTIR] [Ambient-Occlusion] [Voxel-AO]
screen.SVGF=SVGF_SIGMA_Z SVGF_SIGMA_N SVGF_SIGMA_L SVGF_VAR_BOOST SVGF_VAR_HISTNORM SVGF_MIN_LUMA_SIGMA SVGF_ADAPTIVE_SKIP SVGF_SKIP_REL SVGF_WORLD_RADIUS SVGF_SIGMA_WORLD
screen.ReSTIR=RESTIR_GI RESTIR_SCREEN_BOUNCE RESTIR_INITIAL_SAMPLES RESTIR_M_CAP RESTIR_JACOBIAN RESTIR_SPATIAL RESTIR_SPATIAL_SAMPLES RESTIR_SPATIAL_RADIUS RESTIR_COHERENT_TILE RESTIR_W_MAX RESTIR_CLAMP
screen.Ambient-Occlusion=AO_GTAO AO_GI_STRENGTH AO_DIRECT_STRENGTH GTAO_SLICES GTAO_STEPS GTAO_RADIUS
screen.Voxel-AO=VOXEL_AO AO_SAMPLES AO_RADIUS AO_STRENGTH

sliders=METAL_SMOOTHNESS METAL_ROUGHNESS_VARIATION PBR_NORMAL_STRENGTH PBR_DIELECTRIC_NORMAL SPECULAR_SUN_STRENGTH PBR_DIELECTRIC_SUN PBR_DIELECTRIC_REFLECT REFLECTION_SKY_STRENGTH REFLECTION_RAYS REFLECTION_STEPS REFLECTION_REFINE_STEPS REFLECTION_ACCUM_FRAMES REFLECTION_SPATIAL_RADIUS REFLECTION_FIREFLY_CLAMP REFLECTION_SMOOTHNESS_MIN REFLECTION_SKY_FADE RENDER_SCALE TAA_JITTER_SCALE TAA_BLEND_WEIGHT TAA_SHARPNESS VOXEL_DISTANCE LIGHTING_DIRECT LIGHTING_INDIRECT PT_RASTER_AMBIENT_FLOOR sunPathRotation SHADOW_RESOLUTION SHADOW_DISTORT_FACTOR SHADOW_BIAS SHADOW_FILTER_QUALITY SCATTER_AMOUNT VL_STEPS VL_INTENSITY VL_NOON_STRENGTH VL_SUN_RISE_SET_STRENGTH VL_NIGHT_STRENGTH BLOOM_STRENGTH LENS_FLARE_STRENGTH LENS_FLARE_THRESHOLD CHROMATIC_ABERRATION_STRENGTH EXPOSURE AUTO_EXPOSURE_TARGET AUTO_EXPOSURE_SPEED AUTO_EXPOSURE_CENTER_WEIGHT AUTO_EXPOSURE_MIN AUTO_EXPOSURE_MAX COLOR_CONTRAST COLOR_SATURATION COLOR_TEMP PURKINJE_STRENGTH HALATION_STRENGTH SPLIT_TONING_STRENGTH FILM_GRAIN_I OVERWORLD_LUT OVERWORLD_LUT_I AO_SAMPLES AO_RADIUS AO_STRENGTH AO_GI_STRENGTH AO_DIRECT_STRENGTH GTAO_SLICES GTAO_STEPS GTAO_RADIUS GI_RADIUS GI_STRENGTH GI_SKY_BRIGHTNESS GI_SKY_WARMTH SKY_LUT_STEPS GI_BOUNCE_SKY GI_SKY_PROBE_DIST GI_EMISSION GI_SAMPLES GI_ACCUM_FRAMES GI_TEMPORAL_REJECT SVGF_SIGMA_Z SVGF_SIGMA_N SVGF_SIGMA_L SVGF_VAR_BOOST SVGF_MIN_LUMA_SIGMA SVGF_SKIP_REL SVGF_SIGMA_WORLD RESTIR_SCREEN_BOUNCE RESTIR_INITIAL_SAMPLES RESTIR_M_CAP RESTIR_SPATIAL_SAMPLES RESTIR_SPATIAL_RADIUS RESTIR_COHERENT_TILE RESTIR_W_MAX RESTIR_CLAMP SUN_ILLUMINANCE MOON_ILLUMINANCE MIE_G RAYLEIGH_SCATTER_R RAYLEIGH_SCATTER_G RAYLEIGH_SCATTER_B MIE_SCATTER_R MIE_SCATTER_G MIE_SCATTER_B OZONE_PEAK CLOUDS_COVERAGE CLOUDS_DENSITY CLOUDS_LAYER_BOTTOM CLOUDS_LAYER_TOP CLOUDS_WIND_SPEED CLOUDS_WIND_DIR_X CLOUDS_WIND_DIR_Z CLOUDS_PRIMARY_STEPS CLOUDS_LIGHT_STEPS CLOUDS_MS_OCTAVES CLOUDS_MAX_DISTANCE CLOUDS_MIN_TRANSMITTANCE CLOUDS_SHADOW_STEPS CLOUDS_SHADOW_EXTENT WATER_GODRAY_STEPS WATER_GODRAY_STRENGTH WATER_GODRAY_PHASE_G

# Atmosphere rewrite (see shaderpacks/serievx_atmosphere_plan.md):
# colortex12 = packed atmosphere LUT (T + MS + Sky-View) at 256×256
# colortex13 = cloud shadow map at 512×512
texture.colortex12.resolution=256 256
texture.colortex13.resolution=512 512
# Required for the dedicated 3D voxel image (image.voxelImg below). Without this
# Iris refuses to load: "Custom images are being used, but the feature flag ...".
iris.features.required = CUSTOM_IMAGES

# Voxel grid — DEDICATED 3D IMAGE (anisotropic, X/Z = VOXEL_DISTANCE chunks of
# radius, vertical ±64). Explicit dims are honoured by Iris regardless of
# render resolution (unlike texture.colortexN.resolution on an image-bound
# colortex), which is what lets the grid exceed the 2D-atlas / render-res
# limits. Format: <sampler> <pixelFmt> <internalFmt> <type> <clear> <mipmap> X Y Z.
# The #if chain mirrors VOXEL_DISTANCE in options.glsl (dims = chunks*32);
# brick = dims/8, super-brick = dims/64. MUST stay in sync with voxelData.glsl.
#if VOXEL_DISTANCE == 8
    #define VOX_XZ 256
    #define BRICK_XZ 32
    #define SUPER_XZ 4
#elif VOXEL_DISTANCE == 12
    #define VOX_XZ 384
    #define BRICK_XZ 48
    #define SUPER_XZ 6
#elif VOXEL_DISTANCE == 16
    #define VOX_XZ 512
    #define BRICK_XZ 64
    #define SUPER_XZ 8
#elif VOXEL_DISTANCE == 24
    #define VOX_XZ 768
    #define BRICK_XZ 96
    #define SUPER_XZ 12
#else
    #define VOX_XZ 1024
    #define BRICK_XZ 128
    #define SUPER_XZ 16
#endif
image.voxelImg = voxelSampler RGBA_INTEGER RGBA8UI UNSIGNED_BYTE true false VOX_XZ 128 VOX_XZ
# Hierarchical occupancy for the empty-space-skipping DDA. Written by the
# shadow (voxelization) pass alongside every fine voxel store — replaces the
# old prepare2 reduction pass.
image.brickImg = brickSampler RED_INTEGER R8UI UNSIGNED_BYTE true false BRICK_XZ 16 BRICK_XZ
image.superBrickImg = superBrickSampler RED_INTEGER R8UI UNSIGNED_BYTE true false SUPER_XZ 2 SUPER_XZ
# --- Deferred chain: per-pixel ReSTIR GI + SVGF denoiser ---------------------
#   deferred   = d0_restir          (ReSTIR resolve -> colortex3 raw GI + reservoirs 10/11/14/15)
#   deferred1  = d0_accum           (temporal accum + GTAO -> colortex8 GI, colortex9 depth/moments/AO)
#   deferred2  = d0b_historyfix     (disocclusion/edge prefilter -> colortex8)        [GI_DENOISE]
#   deferred3  = d1_atrous_first    (a-trous step 1: colortex8 -> colortex3)          [GI_DENOISE]
#   deferred4  = d1_denoise         (a-trous step 2: colortex3 -> colortex6)          [GI_DENOISE]
#   deferred5  = d2_denoise         (a-trous step 4: colortex6 -> colortex3)          [GI_DENOISE]
#   deferred6  = d3_denoise         (a-trous step 8: colortex3 -> colortex6)          [GI_DENOISE]
#   deferred7  = d4_denoise         (a-trous step 16: colortex6 -> colortex3)         [GI_DENOISE]
#   deferred8  = d7_composite       (scene lighting; reads colortex3 GI when denoised, else colortex8)
#   deferred9  = d7b_reflections    (gated by SPECULAR_REFLECTIONS)
#   deferred10 = d7c_reflection_spatial (gated by SPECULAR_REFLECTIONS)
#   deferred11 = d8_fog_sky
#   deferred12 = d9_vl              (gated by VOLUMETRIC_LIGHT)
program.deferred9.enabled = SPECULAR_REFLECTIONS
program.deferred10.enabled = SPECULAR_REFLECTIONS
program.deferred12.enabled = VOLUMETRIC_LIGHT

# Skip the history-fix prefilter + SVGF a-trous chain when GI_DENOISE is off (the
# passes would otherwise run as full-screen pass-throughs). d7_composite then reads
# the raw temporally-accumulated GI (colortex8) directly.
program.deferred2.enabled = GI_DENOISE
program.deferred3.enabled = GI_DENOISE
program.deferred4.enabled = GI_DENOISE
program.deferred5.enabled = GI_DENOISE
program.deferred6.enabled = GI_DENOISE
program.deferred7.enabled = GI_DENOISE

# Skip the cloud-shadow build pass when CLOUDS_SHADOW is off. d7's
# sampleCloudShadow() already short-circuits to 1.0 in that case, so the
# buffer is never read — prepare1 would just be a wasted dispatch.
program.prepare1.enabled = CLOUDS_SHADOW

# Cloud noise (offline-baked by shaderpacks/volume-noise-generator/).
# cloud_base   (128³ RGBA8): R=perlin-worley shape, G=worley erode mid, B=worley erode hi, A=coverage perlin
# cloud_detail ( 32³ RGBA8): R=curl, G=worley wisp, B=worley sharp wisp, A=boiling perlin
#
# customTexture.<name> binds globally across ALL stages — Iris's per-stage
# `texture.<stage>.<name>` form only accepts reserved sampler names
# (noisetex/colortex0..15/depthtex/shadowtex) or rebinds via .N suffix.
# Custom GLSL uniform names like cloudNoiseBase must use customTexture.
customTexture.cloudNoiseBase=noise/cloud_base.dat TEXTURE_3D RGBA8 128 128 128 RGBA UNSIGNED_BYTE
customTexture.cloudNoiseDetail=noise/cloud_detail.dat TEXTURE_3D RGBA8 32 32 32 RGBA UNSIGNED_BYTE
# Spatiotemporal blue noise (NVIDIA STBN, cosine hemisphere): 128x128 spatial tile x 64 temporal frames.
# RGB = cosine-weighted dir in the +Z hemisphere (decode *2-1); sampled in d0_restir for GI ray directions.
customTexture.blueNoise=noise/stbn_cosine.dat TEXTURE_3D RGBA8 128 128 64 RGBA UNSIGNED_BYTE

# Query LUT atlas. Bound only during composite stages;
# colortex6 is otherwise free for post-processing.
texture.composite.colortex6 = /lib/IMG/Luts2.png
